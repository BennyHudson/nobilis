//** Theme Dependencies **//

"grunt": "^0.4.5",
"grunt-autoprefixer": "^2.2.0",
"grunt-contrib-jshint": "~0.10.0",
"grunt-contrib-nodeunit": "~0.4.1",
"grunt-contrib-sass": "^0.8.1",
"grunt-contrib-uglify": "~0.5.0",
"grunt-contrib-watch": "^0.6.1",
"grunt-git-deploy": "^0.1.1",
"grunt-uncss": "^0.4.2"

//** Instructions **//

Theme file already contains gruntfile.js & package.json
Install dependencies, but no need to run --save-dev

Once all dependencies are installed, run `grunt` to compile, concatenate and watch
